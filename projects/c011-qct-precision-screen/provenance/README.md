# Provenance

The official HUD workbooks are treated as immutable. `raw_input_manifest.json` records
cryptographic identity. `environment.txt` records the execution platform. The workbook
structure audit checks hidden sheets and defined names without editing the originals.

The post-D036 handoff is authoritative for remembered regression anchors, but anchors do
not determine implementation choices.
