# 2022 provenance finding

The official workbook stores the disputed Los Angeles records as follows:

- `060372014010`: poverty releases are float32-like values for 0.286, 0.237 and 0.192;
  official QCT is zero.
- `060375432020`: poverty releases are float32-like values for 0.205, 0.236 and 0.236;
  official QCT is one.

All sheets are visible; there are no defined names or comment/thread parts that reveal a
hidden ranking rule. The cached workbook values therefore confirm the displayed three-
decimal inputs but do not expose a pre-rounded poverty value that resolves the cap-boundary
swap. A local exception is prohibited, so provenance remains unresolved.
